export default function HomePage() {
  return (
    <main className="min-h-screen bg-slate-950 text-white">
      <section className="max-w-6xl mx-auto px-6 pt-28 pb-16 text-center">
        {/* ARIA bien grande */}
        <div className="text-7xl md:text-8xl lg:text-9xl font-extrabold tracking-tight bg-gradient-to-r from-white to-indigo-300 bg-clip-text text-transparent leading-[0.9]">
          ARIA
        </div>

        {/* Tagline */}
        <h2 className="mt-6 text-3xl md:text-4xl font-bold">
          The Future of Hiring is Here
        </h2>
        <p className="mt-4 text-white/70 max-w-3xl mx-auto">
          ARIA delivers AI-powered voice interviews, predictive analytics, and automated candidate assessment.
          Save hiring time while improving accuracy — with multilingual interviewers ready out of the box.
        </p>

        {/* CTAs */}
        <div className="mt-10 flex items-center justify-center gap-3">
          <a
            href="/dashboard"
            className="px-5 py-3 rounded-lg bg-indigo-600 hover:bg-indigo-500"
          >
            Get Started
          </a>
          <a
            href="/pricing"
            className="px-5 py-3 rounded-lg bg-white/10 hover:bg-white/20"
          >
            Pricing
          </a>
        </div>
      </section>

      {/* Feature highlights */}
      <section className="max-w-6xl mx-auto px-6 pb-24 grid md:grid-cols-3 gap-6">
        <div className="rounded-xl border border-white/10 bg-white/[0.03] p-6">
          <h3 className="font-semibold mb-2">AI Voice Interviewers</h3>
          <p className="text-white/70">
            Male/Female voices in English and Spanish. Up to 10 tailored questions per role.
          </p>
        </div>
        <div className="rounded-xl border border-white/10 bg-white/[0.03] p-6">
          <h3 className="font-semibold mb-2">Smart CV + JD Matching</h3>
          <p className="text-white/70">
            RAG + embeddings to evaluate candidates beyond keyword matching.
          </p>
        </div>
        <div className="rounded-xl border border-white/10 bg-white/[0.03] p-6">
          <h3 className="font-semibold mb-2">Enterprise Ready</h3>
          <p className="text-white/70">
            Audit trails, integrations (LinkedIn, Retell), and usage-based billing.
          </p>
        </div>
      </section>
    </main>
  );
}
